---
const { href } = Astro.props;
const currentPath = Astro.url.pathname;
const active = currentPath.startsWith(href);
---

<a
  href={href}
  class=`p-2 rounded-sm font-semibold text-gray-300 hover:text-slate-50 ${active ? "bg-slate-300 dark:bg-slate-800" : ""}`
  ><slot /></a
>
