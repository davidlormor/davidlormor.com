---
const { href } = Astro.props;
const page = Astro.url.pathname;
---

<a
  href={href}
  class=`p-2 rounded-sm font-semibold text-gray-300 hover:text-slate-50 ${page === href ? "bg-slate-300 dark:bg-slate-800" : ""}`
  ><slot /></a
>
